{
  /* <div className="flex space-x-4 border-b border-gray-200">
                  <button
                    className={`py-2 px-4 text-xs font-medium focus:outline-none ${
                      activeTab === "Details"
                        ? "text-white bg-blue-500 rounded-t-md"
                        : "text-gray-600 hover:text-blue-500 hover:bg-gray-100 rounded-t-md"
                    } transition duration-200`}
                    onClick={() => setActiveTab("Details")}
                  >
                    Details
                  </button>
                  <button
                    className={`py-2 px-4 text-xs font-medium focus:outline-none ${
                      activeTab === "Change Status"
                        ? "text-white bg-blue-500 rounded-t-md"
                        : "text-gray-600 hover:text-blue-500 hover:bg-gray-100 rounded-t-md"
                    } transition duration-200`}
                    onClick={() => setActiveTab("Change Status")}
                  >
                    Change Status
                  </button>
                  <button
                    className={`py-2 px-4 text-xs font-medium focus:outline-none ${
                      activeTab === "Edit"
                        ? "text-white bg-blue-500 rounded-t-md"
                        : "text-gray-600 hover:text-blue-500 hover:bg-gray-100 rounded-t-md"
                    } transition duration-200`}
                    onClick={() => setActiveTab("Edit")}
                  >
                    Edit
                  </button>
                </div> */
}
